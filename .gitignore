# 환경 변수 (절대 커밋 금지)
.env
.env.local
*.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
wheels/
*.spec

# 가상환경
.venv/
venv/
env/
ENV/

# 런타임 상태 파일 (실험 결과이므로 제외)
nodes.json
edges.json
state.json

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# 테스트 / 커버리지
.pytest_cache/
.coverage
htmlcov/

# LangChain / LangSmith 캐시
.langchain.db

# 데이터 파이프라인 중간 산출물
data-team/data/raw/*
data-team/data/output/*
!data-team/data/raw/.gitkeep
!data-team/data/output/.gitkeep

# Worktree 전용 (경로가 워크트리마다 다름 — worktree-up.sh 가 자동 관리)
agent-platform/infra/docker-compose.worktree.yml
