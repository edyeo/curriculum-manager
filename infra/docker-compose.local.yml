version: '3.9'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: curriculum-postgres
    environment:
      POSTGRES_DB: curriculum_db
      POSTGRES_USER: curriculum_user
      POSTGRES_PASSWORD: curriculum_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/schemas/:/docker-entrypoint-initdb.d/
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U curriculum_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: curriculum-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Curriculum Manager Agent
  curriculum-manager:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: curriculum-manager
    container_name: curriculum-manager
    environment:
      DB_TYPE: file
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: curriculum_db
      DB_USER: curriculum_user
      DB_PASSWORD: curriculum_password
      REDIS_HOST: redis
      REDIS_PORT: 6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-sk-test-key}
      LOG_LEVEL: DEBUG
    ports:
      - "8001:8001"
    depends_on:
      - redis
    volumes:
      - ../agent-platform/agents/curriculum-manager:/app/agents/curriculum-manager
      - ../agent-platform/shared:/app/shared
      - ../nodes.json:/app/nodes.json
      - ../edges.json:/app/edges.json
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Mental Model Manager Agent
  mental-model-manager:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: mental-model-manager
    container_name: mental-model-manager
    environment:
      AGENT: mental-model-manager
      PORT: 8002
      DB_TYPE: file
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: curriculum_db
      DB_USER: curriculum_user
      DB_PASSWORD: curriculum_password
      REDIS_HOST: redis
      REDIS_PORT: 6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-sk-test-key}
      LOG_LEVEL: DEBUG
    ports:
      - "8002:8002"
    depends_on:
      - redis
    volumes:
      - ../agent-platform/agents/mental-model-manager:/app/agents/mental-model-manager
      - ../agent-platform/shared:/app/shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Researcher Agent
  researcher:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: researcher
    container_name: researcher
    environment:
      AGENT: researcher
      PORT: 8003
      DB_TYPE: file
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: curriculum_db
      DB_USER: curriculum_user
      DB_PASSWORD: curriculum_password
      REDIS_HOST: redis
      REDIS_PORT: 6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-sk-test-key}
      LOG_LEVEL: DEBUG
      NODES_FILE: /app/nodes.json
      RESEARCH_FILE: /app/research_results/research_results.json
    ports:
      - "8003:8003"
    depends_on:
      - redis
    volumes:
      - ../agent-platform/agents/researcher:/app/agents/researcher
      - ../agent-platform/shared:/app/shared
      - ../nodes.json:/app/nodes.json
      - research_results:/app/research_results
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Question Generator Agent
  question-generator:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: question-generator
    container_name: question-generator
    environment:
      AGENT: question-generator
      PORT: 8004
      DB_TYPE: file
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: curriculum_db
      DB_USER: curriculum_user
      DB_PASSWORD: curriculum_password
      REDIS_HOST: redis
      REDIS_PORT: 6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-sk-test-key}
      LOG_LEVEL: DEBUG
    ports:
      - "8004:8004"
    depends_on:
      - redis
    volumes:
      - ../agent-platform/agents/question-generator:/app/agents/question-generator
      - ../agent-platform/shared:/app/shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Grader Agent
  grader:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: grader
    container_name: grader
    environment:
      AGENT: grader
      PORT: 8005
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o}
      LOG_LEVEL: DEBUG
    ports:
      - "8005:8005"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8005/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Virtual Student Agent
  virtual-student:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: virtual-student
    container_name: virtual-student
    environment:
      AGENT: virtual-student
      PORT: 8006
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o}
      LOG_LEVEL: DEBUG
    ports:
      - "8006:8006"
    volumes:
      - ../agent-platform/agents/virtual-student:/app/agents/virtual-student
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8006/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # API Gateway
  gateway:
    build:
      context: ../agent-platform
      dockerfile: Dockerfile
      args:
        AGENT: gateway
    container_name: api-gateway
    environment:
      AGENT: gateway
      PORT: 9000
      CURRICULUM_MANAGER_URL: http://curriculum-manager:8001
      MENTAL_MODEL_MANAGER_URL: http://mental-model-manager:8002
      RESEARCHER_URL: http://researcher:8003
      QUESTION_GENERATOR_URL: http://question-generator:8004
      GRADER_URL: http://grader:8005
      LOG_LEVEL: DEBUG
    ports:
      - "9000:9000"
    depends_on:
      - curriculum-manager
      - mental-model-manager
      - researcher
      - question-generator
      - grader
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Contents Manager Backend
  contents-manager-backend:
    build:
      context: ../apps/contents-manager/backend
      dockerfile: Dockerfile
    container_name: contents-manager-backend
    environment:
      GATEWAY_URL: http://api-gateway:9000
      AGENT_DATA_DIR: /app/data
      DATABASE_URL: ${CM_DATABASE_URL:-sqlite:////app/cm_db/contents_manager.db}
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@example.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin1234}
      KG_SERVICE_TOKEN: ${KG_SERVICE_TOKEN:-kg-service-secret}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o}
      ONTOLOGY_PATH: /app/data/ontology.yaml
    ports:
      - "8010:8010"
    depends_on:
      - gateway
    volumes:
      - ../nodes.json:/app/data/nodes.json
      - ../edges.json:/app/data/edges.json
      - ../agent-platform/shared/ontology.yaml:/app/data/ontology.yaml:ro
      - cm_db:/app/cm_db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8010/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Contents Manager Frontend
  contents-manager-frontend:
    build:
      context: ../apps/contents-manager/frontend
      dockerfile: Dockerfile
    container_name: contents-manager-frontend
    ports:
      - "3000:3000"
    depends_on:
      - contents-manager-backend
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Student Platform Backend
  student-platform-backend:
    build:
      context: ../apps/student-platform/backend
      dockerfile: Dockerfile
    container_name: student-platform-backend
    environment:
      KG_API_URL: http://contents-manager-backend:8010
      KG_SERVICE_TOKEN: ${KG_SERVICE_TOKEN:-kg-service-secret}
      STUDENT_PLATFORM_URL: http://student-platform-backend:8020
      GATEWAY_URL: http://api-gateway:9000
      STUDENT_JWT_SECRET: ${STUDENT_JWT_SECRET:-student-secret-change-in-prod}
      DATABASE_URL: sqlite:////app/db/student_platform.db
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    ports:
      - "8020:8020"
    depends_on:
      - contents-manager-backend
      - gateway
    volumes:
      - student_db:/app/db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8020/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Student Platform Frontend
  student-platform-frontend:
    build:
      context: ../apps/student-platform/frontend
      dockerfile: Dockerfile
    container_name: student-platform-frontend
    environment:
      API_PROXY_TARGET: http://student-platform-backend:8020
    ports:
      - "3001:3001"
    depends_on:
      - student-platform-backend
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Virtual Student API
  virtual-student-api:
    build:
      context: ../apps/student-platform/virtual-student-api
      dockerfile: Dockerfile
    container_name: virtual-student-api
    environment:
      DATABASE_URL: sqlite:////app/data/virtual_students.db
      KG_API_URL: http://contents-manager-backend:8010
      KG_SERVICE_TOKEN: ${KG_SERVICE_TOKEN:-kg-service-secret}
      STUDENT_PLATFORM_URL: http://student-platform-backend:8020
      GATEWAY_URL: http://api-gateway:9000
      VIRTUAL_STUDENT_AGENT_URL: http://virtual-student:8006
    ports:
      - "8030:8030"
    depends_on:
      - contents-manager-backend
      - student-platform-backend
      - virtual-student
    volumes:
      - virtual_student_db:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8030/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Virtual Student UI
  virtual-student-ui:
    build:
      context: ../apps/student-platform/virtual-student-ui
      dockerfile: Dockerfile
    container_name: virtual-student-ui
    ports:
      - "3010:3010"
    depends_on:
      - virtual-student-api
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3010"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Neo4j Graph Database
  neo4j:
    image: neo4j:5-community
    container_name: curriculum-neo4j
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-curriculum_password}
      NEO4J_server_memory_heap_initial__size: 512m
      NEO4J_server_memory_heap_max__size: 1G
    ports:
      - "7474:7474"   # Browser UI
      - "7687:7687"   # Bolt
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "${NEO4J_PASSWORD:-curriculum_password}", "RETURN 1"]
      interval: 15s
      timeout: 10s
      retries: 5

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  student_db:
    driver: local
  research_results:
    driver: local
  cm_db:
    driver: local
  virtual_student_db:
    driver: local
  neo4j_data:
    driver: local

networks:
  default:
    name: curriculum-network
    driver: bridge
